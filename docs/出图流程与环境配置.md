# 出图流程与环境配置（商品图自动化）

本文件让**任何新会话**(云端或本地)都能无缝接手"把进货原图做成白底商品图并存进 GitHub"的流程。

## 一、Riverflow 关键参数

| 项 | 值 |
|---|---|
| Riverflow brand_id | `5f98f8b4-3335-489e-ac4b-37024dc2a2ae` |
| 当前品牌名 | **Brilliant Earth**(占位/测试品牌;建议后续在 Riverflow 新建 "1999" 品牌) |
| 出图模型 | `riverflow-2.5-pro-high` |
| 构图规则 | **戒指横放平视 + 横构图 4:3**(见 `.claude/agents/diamond-studio.md` 规则 2.5) |

## 二、网络放行要求(方案一)

云端环境要能把 Riverflow 成图存进 GitHub,必须在环境网络策略里放行:

- `riverflow.ai`
- `*.riverflow.ai`
- `storage.googleapis.com`（默认已放行,保留"include default list"勾选即可）

设置位置:claude.ai/code → 环境(Default)→ **Network access = Custom** → **Allowed domains** 逐行填写 → 保存 → **新开会话生效**。

> 未放行 riverflow.ai 时:能生成、能在 Riverflow 查看,但**下载不进 git**(成图仅走 www.riverflow.ai 代理链接,被出口封锁)。

## 三、全自动出图管线(放行后)

前提:创始人把原图放进 **Google Drive**(Google 域名默认放行,可直接拉取)。

1. **取图**:用 Google Drive 工具 `search_files` / `download_file_content` 拉取原图(base64)→ 解码写入本地 `refs/`(不入库)
2. **上传 Riverflow**:`riverflow_start_image_upload`(brand_id 见上)→ PUT 原图到返回的 upload_url → `riverflow_get_image_upload` 拿 `asset_id`
3. **生成**:`riverflow_create_uploaded_asset_image_edit`
   - `source_asset_id` = 上一步 asset_id
   - `aspect_ratio: "4:3"`、`resolution: "2K"`、`model_key: "riverflow-2.5-pro-high"`
   - instruction 强调:**戒指横放平视、戒圈横向、主石朝上、白底、去手去背景、微距锐利**
4. **下载**:轮询 `riverflow_download_asset_image`(asset_type=edit)→ 拿到 `direct_url`(www.riverflow.ai)→ curl 下载到 `docs/images/<条码>.png`
5. **入库**:提交 `docs/images/` + 在 `docs/样品拿货思路.md` 的商品图表登记(条码、asset_id、状态)
6. **对应**:原图按**条码命名**(如 `00412176.jpg`)以自动对应进货单行

## 四、命名与 Drive 约定

- Drive 文件夹建议名:**`1999进货`**
- 每款一张最清晰正面图,文件名用**条码号**(多张加 `-a`/`-b`)
- 17 款条码见 `docs/样品清单.md` 逐件清单

## 五、已完成

- #1 祖母绿三石戒（条码 00412176）横版商品图已生成,存于 Riverflow(edit asset `6208bf74-1655-4637-a576-a59f5c2dee8f`);待 riverflow.ai 放行后可下载入库。
